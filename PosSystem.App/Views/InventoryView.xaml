<UserControl x:Class="PosSystem.App.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Inventory Management" FontSize="20" FontWeight="Bold"/>
            <Separator Width="400" Background="Transparent"/>
            <Button Content="Receive Stock" Command="{Binding ShowReceiveStockCommand}" 
                    Margin="10,0,0,0" Width="120" Height="30"/>
        </StackPanel>

        <!-- Inventory List -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" AutoGenerateColumns="False" 
                  Margin="0,10" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="120"/>
                <DataGridTextColumn Header="Current Stock" Binding="{Binding StockQuantity}" Width="100"/>
                <DataGridTextColumn Header="Low Stock" Binding="{Binding LowStockThreshold}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StockStatus}" Width="100"/>
                <DataGridTextColumn Header="Last Received" Binding="{Binding LastStockUpdate, StringFormat=yyyy-MM-dd}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Stock History -->
        <GroupBox Grid.Row="2" Header="Stock Receiving History">
            <DataGrid ItemsSource="{Binding StockHistory}" AutoGenerateColumns="False" Height="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding ReceiveDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                    <DataGridTextColumn Header="Product" Binding="{Binding Product.Name}" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding QuantityReceived}" Width="80"/>
                    <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="150"/>
                    <DataGridTextColumn Header="Cost" Binding="{Binding UnitCost, StringFormat=C}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>