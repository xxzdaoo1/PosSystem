<UserControl x:Class="PosSystem.App.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PosSystem.App.Views"
             xmlns:viewmodels="clr-namespace:PosSystem.App.ViewModels" 
             xmlns:helpers="clr-namespace:PosSystem.App.Helpers"
             d:DataContext="{d:DesignInstance Type=viewmodels:SalesViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Style PlaceHolder Textbox -->
        <Style x:Key="placeHolder" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Grid>
                            <TextBox Text="{Binding Path=Text,
                                                RelativeSource={RelativeSource TemplatedParent}, 
                                                Mode=TwoWay,
                                                UpdateSourceTrigger=PropertyChanged}"
                                 x:Name="textSource" 
                                 Background="Transparent" 
                                 Panel.ZIndex="2" />
                            <TextBox Text="{TemplateBinding Tag}" Background="{TemplateBinding Background}" Panel.ZIndex="1">
                                <TextBox.Style>
                                    <Style TargetType="{x:Type TextBox}">
                                        <Setter Property="Foreground" Value="Transparent"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Text, Source={x:Reference textSource}}" Value="">
                                                <Setter Property="Foreground" Value="LightGray"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Sales" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>

        <!-- Left Panel - Product Search & Selection -->
        <GroupBox Grid.Row="1" Grid.Column="0" Header="Products" Margin="0,0,5,0">
            <DockPanel>
                <!-- Search Box -->
                <TextBox DockPanel.Dock="Top" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource placeHolder}" Tag="Enter product name or barcode" 
                         Margin="0,0,0,5" Height="25" />

                <!-- Product List -->
                <ListBox ItemsSource="{Binding ViewBindToSalesModel.FilteredProducts}" 
                         SelectedItem="{Binding ViewBindToSalesModel.SelectedProduct}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="2">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Price, StringFormat=C}" Foreground="Green"/>
                                <TextBlock Text="{Binding StockQuantity, StringFormat='Stock: {0}'}" 
                                           Foreground="Gray" FontSize="10"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </GroupBox>

        <!-- Right Panel - Cart & Transaction -->
        <GroupBox Grid.Row="1" Grid.Column="1" Header="Shopping Cart" Margin="5,0,0,0">
            <DockPanel>
                <!-- Cart Items -->
                <DataGrid DockPanel.Dock="Top" ItemsSource="{Binding ViewBindToSalesModel.CartItems}" 
                          AutoGenerateColumns="False" Height="250" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding Product.Name}" Width="*"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding UnitPrice, StringFormat=C}" Width="80"/>

                        <!-- Quantity Column with +/- Buttons -->
                        <DataGridTemplateColumn Header="Qty" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="-" Width="25" Height="20" Margin="2"
                            Command="{Binding DataContext.DecreaseQuantityCommand, 
                                     RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding}"/>
                                        <TextBlock Text="{Binding Quantity}" Width="30" TextAlignment="Center" 
                               VerticalAlignment="Center" FontWeight="Bold"/>
                                        <Button Content="+" Width="25" Height="20" Margin="2"
                            Command="{Binding DataContext.IncreaseQuantityCommand, 
                                     RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice, StringFormat=C}" Width="80"/>
                        <DataGridTemplateColumn Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remove" 
                        Command="{Binding DataContext.RemoveFromCartCommand, 
                                 RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding}"
                        Margin="2" Height="20"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Totals -->
                <StackPanel DockPanel.Dock="Bottom" Margin="0,10,0,0">
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <TextBlock Text="Subtotal:" FontWeight="Bold" Width="80"/>
                        <TextBlock Text="{Binding ViewBindToSalesModel.Subtotal, StringFormat=C}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <TextBlock Text="Tax (8%):" FontWeight="Bold" Width="80"/>
                        <TextBlock Text="{Binding ViewBindToSalesModel.TaxAmount, StringFormat=C}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="TOTAL:" FontWeight="Bold" FontSize="14" Width="80"/>
                        <TextBlock Text="{Binding ViewBindToSalesModel.TotalAmount, StringFormat=C}" 
                                   FontWeight="Bold" FontSize="14" Foreground="Green"/>
                    </StackPanel>
                </StackPanel>
            </DockPanel>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="Add to Cart" Command="{Binding AddToCartCommand}" 
                    Width="100" Height="35" Margin="0,0,10,0"
                    CommandParameter="{Binding ViewBindToSalesModel.SelectedProduct}"/>
            <Button Content="Process Payment" Command="{Binding ProcessPaymentCommand}" 
                    Width="120" Height="35" Margin="0,0,10,0" FontWeight="Bold"
                    Background="Green" Foreground="White"/>
            <Button Content="Cancel Sale" Command="{Binding CancelSaleCommand}" 
                    Width="100" Height="35" Background="LightCoral"/>
        </StackPanel>
    </Grid>
</UserControl>